# IDE
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
venv/
.venv/
.ipynb_checkpoints/

# Environment variables
.env
.env.*

# GCP credentials / secrets
key.json
credentials.json
application_default_credentials.json
service-account.json
*.json.secret
*gcp-key*.json
*service-account*.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
key.json

# Part 5: large data files pulled from GCS for local module-import test
# (the production container fetches these from GCS at runtime; never commit them to git)
week2/data/processed/

# Week 4: generated metrics report (regenerated each run, uploaded as a CI artifact)
week4/metrics-*.json

# Week 6: local report material (demo output, screenshots) — never pushed
week6/Report/
