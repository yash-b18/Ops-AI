# IDE
.vscode/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
venv/
.venv/
.ipynb_checkpoints/

# Environment variables
.env
.env.*

# GCP credentials / secrets
key.json
credentials.json
application_default_credentials.json
service-account.json
*.json.secret
*gcp-key*.json
*service-account*.json

# OS
.DS_Store
Thumbs.db

# Logs
*.log
key.json

# Week 3 working notes (local only)
week3/NOTES.md

# Part 5: large data files pulled from GCS for local module-import test
# (the production container fetches these from GCS at runtime; never commit them to git)
week2/data/processed/

# Part 7: REPORT.md is the local-only graded deliverable.
# User submits via a separate mechanism (not via `git push`).
week3/REPORT.md
